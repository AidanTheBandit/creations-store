import { CreationApp } from "./creation-app";

// /creation — the Creations Store running on the R1. Client-side state machine
// (boot → link → list/experience). See creation-app.tsx.
export default function CreationPage() {
  return <CreationApp />;
}
